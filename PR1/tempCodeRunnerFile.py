    @staticmethod
    def new_mine(tree, node, support_count, confidence_pct, prefix=set(),all_p={})
        for item in tree.f1_sorted[:-1]:
            build tree
                update fp
                    for each item in f1 sorted 